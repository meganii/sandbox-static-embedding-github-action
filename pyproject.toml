[project]
name = "sandbox-static-embedding-github-action"
version = "0.1.0"
description = "Add your description here"
readme = "README.md"
requires-python = ">=3.12"
dependencies = [
    "duckdb>=1.4.1",
    "pandas>=2.3.3",
    "sentence-transformers>=5.1.1",
]
